# Dynamic Data Table Manager — Core Version

This project includes the required **core features**:

### ✅ Features
- Default columns: **Name, Email, Age, Role**
- Sorting (ASC/DESC)
- Global search across all fields
- Pagination (10 rows per page)
- Manage Columns Modal  
  - Show/Hide columns  
  - Add new columns dynamically  
- Import CSV (using PapaParse)
- Export CSV (using FileSaver)
- Column visibility saved in localStorage

### 🚀 Run Locally
1. Install dependencies:  
   ```bash
   npm install
